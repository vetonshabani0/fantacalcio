import { MyLeaguesIntro } from "@/components/MyLeaguesIntro";

export const metadata = { title: "Le mie leghe — FantaLive" };

export default function MyLeaguesPage() {
  return <MyLeaguesIntro />;
}
